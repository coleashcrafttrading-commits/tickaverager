.env
.venv/
__pycache__/
state/
*.log
.env.*
_jscheck.js
*.bak
ops/
reports/
_backup_*/

# research data caches: tick/tape/float/news snapshots and daily tables (GBs) -- never in git
research/ross/
research/float/
research/news/
research/scanner/
# options bars: a strike grid x every expiry x 19 months. Hundreds of MB, and
# rebuildable from Alpaca with optfetch.py, so it is a cache and not a source.
research/options/

# per-machine, written by the app at runtime -- template is config.example.json
config.json
.env.bak*

# agent worktrees (isolated checkouts) are never part of the repo
.claude/worktrees/
